import { render, screen } from '@testing-library/react';
// import App from './App';

describe('are there any tests?', () => {
    it('has tests', () => {
        expect(true).toBe(true);
    });
});
